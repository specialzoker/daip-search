# 대입통합검색기 — 작업 인수인계 노트

## 프로젝트 개요

- **사이트**: https://specialzoker.github.io/daip-search/
- **GitHub**: https://github.com/specialzoker/daip-search
- **로컬 경로**: `C:\Users\user\search`
- **사용자**: 광명북고 진로진학부장 정현석 (specialzoker@gmail.com)
- **용도**: 학생 내신·수능 입력 → 합격 가능 대학·학과 추천 + 실제 합격 사례 통계

## 기술 스택

- 순수 HTML/JS/CSS (단일 파일 `index.html` ~1500줄)
- 빌드 시스템 없음. GitHub Pages에 직접 호스팅
- Firebase Auth (이메일 화이트리스트 로그인)
- 데이터: 로컬 CSV/JSON 파일 (구글 시트 의존성 제거됨)

## 디렉토리 구조

```
C:\Users\user\search\
├── index.html                          # 단일 파일 SPA (모든 코드)
├── data/
│   ├── main.csv                        # 메인 입결 데이터 (3,933행)
│   ├── min.csv                         # 수능 최저학력기준 (49행)
│   └── jong.csv                        # 전형방법/종합전형 상세 (72행)
├── cases.json                          # 합격 사례 통계 (2.9MB, 8,355 학과)
├── grade5.json                         # 5등급↔9등급 변환표 (34KB)
├── 대입통합검색기_데이터템플릿.xlsx     # ★ 데이터 편집 원본 (현재 사용)
├── 대입통합검색기_데이터정리본.xlsx     # 구버전 (2026.6 이전, 미사용)
├── build_csv.py                        # xlsx → data/*.csv 변환
├── aggregate.py                        # 수시사례 376k → cases.json 집계
└── ONBOARDING.md                       # 이 파일
```

## 데이터 업데이트 워크플로

### 메인 데이터 (수시·정시 입결, 최저기준, 전형방법)
1. `대입통합검색기_데이터템플릿.xlsx` 편집
   - 시트: `메인` → main.csv, `최저기준` → min.csv, `전형방법` → jong.csv
   - 메인 시트에 전형별 `충원` 열 있음 (추가합격 인원). build_csv.py가 빌드 시
     `교과1/충원`·`종합1/충원`·`정시/충원` 등으로 자동 고유화 → 모달 전형 카드에 "충원 N명" 표시
   - `모집인원_교과/종합/정시`가 있으면 카드에 충원율(충원/모집×100%)도 함께 표시 (100% 초과 정상)
2. `python build_csv.py` 실행 → `data/*.csv` 생성
3. `git add data/ && git commit -m "데이터 업데이트" && git push`

### 합격 사례 통계 (재집계 필요할 때만)
- 원본: `C:\클라우드파일\다운 받은 파일\수시사례데이터통합_2.xlsx` (427,997행, 2026-06 기준)
  - 이전 버전: `수시사례데이터통합_260423.xlsx` (376,699행)
- 새 파일이 오면 `aggregate.py`의 `SRC` 경로만 갱신 후 `python aggregate.py` 실행
- 42만 행 기준 처리 ~3~5분 소요

## 주요 기능 (구현 완료)

### 1. 검색·판정 시스템
- 내신 등급 + 수능 백분위 입력 → 학과별 합격 가능성 판정
- 판정 단계: 안정 / 가능 / 경계 / 위험 / 그러지마
- 교과·종합·정시 3개 전형 동시 판정

### 2. 필터
- **권역**: 서울·경인·충청·경상·전라·강원 (단일 선택)
- **계열**: 전체·자연·인문·공통·예체능 (단일 선택)
- **전형 다중선택**: 교과/종합/정시 (다중 선택)
- **판정 다중선택**: 안정/가능/경계/위험 (다중 선택)
- **여유 슬라이더**: 합격선보다 너무 점수 남는 학과 자동 제외
  - 0~50 슬라이더 (50=무제한, 0=딱맞음)
  - 슬라이더 값 / 10 = 등급 단위 한계
  - 정시는 1등급 ≈ 5% 환산 적용
- **키워드 검색**: 대학·학과명

### 3. 모달 상세 화면
- 교과·종합·정시 카드 (합격선 70%/50%, 내 내신, 여유 등급)
- 영어·한국사 등급 표시 (정시)
- **종합전형 정보**: 1단계발표·면접일자·평가요소(역량 %)·인재상
- **실제 합격 사례**: 박스플롯 + 평균/중앙값/p25~p75, 본인 위치 표시
- **최저기준**: 결과 표 우측에 컬럼으로 표시
- 2025→2026 변경사항

### 4. 5등급제(1·2학년) 지원
- 학년 토글: 3학년(9등급) / 2학년·1학년(5등급)
- 1·2학년 선택 시:
  - 입력 라벨이 "5등급 내신"으로 변경
  - 자동으로 9등급 환산하여 비교
  - 반영조합 선택: 자동(계열별) / 전과목 / 국수영사(인문/예체능) / 국수영과(자연) / 국수영사과
- 학년 선택 localStorage 저장

### 5. 합격 사례 통계
- 원본: 수시사례 376,699건
- 학과별 집계 (5,774 학과)
- 추합 포함 (합 + 추합 = 합격으로 카운트)
- 등급 컬럼:
  - 교과: 계열별 환산 (인문/예체능=국영수사, 자연=국영수과)
  - 종합: 전교과 100%
  - 정시: 카운트만 (등급 통계 없음)
- 매칭: 대학명 + 모집단위
  - 정규화: `여대↔여자대`, `학교/대학` 접미사 제거, 공백 제거
  - 분교/계열 자동 합산 (`연세대(미래) + 연세대(서울)` 등)

## 매칭 함수들 (중요)

### normCaseUniv (cases.json용)
```js
function normCaseUniv(n) {
  return String(n||'').replace(/\(.*$/, '')
    .replace(/여자대(학교)?$/,'여대')
    .replace(/대학교$/,'대').replace(/\s/g,'').trim();
}
```

### lookupJong (종합전형용)
```js
function normUnivKey(n) { return n.replace(/학교.*$/,'').replace(/대학$/,'대').replace(/\s/g,''); }
function normName(n) { return n.replace(/전형$|형$/,'').replace(/\s/g,''); }
```

## 컬럼 매핑 (메인 시트)

```js
const C = {
  kwon:'권역', region:'지역', sub:'세부지역',
  univ:'대학명', dept26:'2026 수시 모집단위', dept25:'2025 수시 모집단위',
  series:'계열',
  g1t:'교과1/전형', g1_50:'교과1/50%', g1_70:'교과1/70%',
  g2t:'교과2/전형', g2_50:'교과2/50%', g2_70:'교과2/70%',
  g3t:'교과3/전형', g3_50:'교과3/50%', g3_70:'교과3/70%',
  j1t:'종합1/전형', j1_50:'종합1/50%', j1_70:'종합1/70%',
  j2t:'종합2/전형', j2_50:'종합2/50%', j2_70:'종합2/70%',
  su_pct:'정시백분위', su_area:'2025 정시 반영영역', su_enghis:'영어한국사',
  chg_type:'전형변경', chg_dept:'모집단위변경',
};
```

## 판정 임계값

```js
const THR = {
  grade: { stable:-0.5, safe:0, border:0.3, warn:0.7 },  // 등급 (교과/종합)
  pct:   { stable:5.0,  safe:2.0, border:-2.0, warn:-5.0 }, // 백분위 (정시)
};
```

## 알려진 이슈 / 개선 가능 사항

### 보안 (TODO)
- `cases.json` 1.8MB가 GitHub Pages에서 누구나 다운로드 가능
- 사용자가 인지함. 해결 방안 (난이도 순):
  1. Firebase Auth 통과 후에만 로드 (URL 직접 접근은 여전히 가능)
  2. Firebase Storage에 옮기고 인증된 사용자만 접근
  3. Cloudflare Worker로 API 게이트웨이 구축
  4. 데이터를 더 거칠게 집계 (학과별 → 권역+계열)

### 데이터 매칭 한계
- 종합전형 시트의 전형명이 메인 시트와 다를 수 있음 (정규화로 어느정도 해결)
- 합격사례 데이터의 분교 처리 (`서울대(자연)`, `서울대(인문)` 등) → base 이름으로 합산

### 미구현
- 케이스 데이터를 전형명별로 세분화 (현재는 교과/종합/정시로만 분류)
- 미래 컬럼(모집인원·반영비율·3년추이·경쟁률) 활용

## Git 컨벤션

- 한글 커밋 메시지 사용
- 변경사항 푸시 후 사용자가 GitHub Pages 1~2분 대기 후 Ctrl+Shift+R로 확인
- 커밋 시 git config는 이미 설정됨 (specialzoker / specialzoker@gmail.com)

## 사용자 작업 스타일

- 한국어로 대화
- 화면 스크린샷으로 버그·요청사항 전달 많음
- 데이터 매칭 안 되면 시트의 정확한 표기 확인 필요
- 기능을 한 번에 하나씩 점진적으로 추가하는 걸 선호
- 디버그 코드는 작업 끝나면 반드시 제거 요청함

## 다음 세션에서 작업 시작 시 체크리스트

1. `index.html`의 현재 상태 확인 (특히 `loadCsvFromStorage` 함수, `doSearch` 필터 로직)
2. `data/*.csv` 파일 존재 확인
3. `git log -10`으로 최근 작업 흐름 파악
4. 사용자 요청 사항 듣고 → 관련 부분만 부분 Read → Edit
5. 푸시 후 사용자에게 "Ctrl+Shift+R 후 확인" 안내

## 자주 참조하는 라인 (인덱스)

- 정시 다중 행 처리: doSearch 함수 내 `// ── 정시 ──` 섹션
- 5등급 환산: `convert5to9`, `getEffNaesin`, `effNaesinFor`
- 필터: `fRegion`, `fSeries`, `fTypes`, `fVerdict`, `fMargin`
- 모달: `showDetail` 함수 (~1300줄대)
- 합격사례 표시: `lookupCases`, modal 안의 `cs-card` 렌더링
